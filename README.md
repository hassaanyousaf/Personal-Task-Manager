# Personal-Task-Manager
Full-stack Task Manager built with Django REST Framework, JWT auth, &amp; modern UI. Features CRUD operations, dark theme, responsive design with clean separation of concerns.
